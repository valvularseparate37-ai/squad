# 2026-03-25T18:11 — EECOM (Core Dev, sonnet)

**Task:** Fix #590 P0 getPersonalSquadRoot() bug  
**Mode:** background  
**Status:** ✅ MERGED

## Outcome

Fixed path mismatch in personal squad resolution. Committed on `squad/590-fix-personal-squad-root`.

## Details

**Root cause:** `getPersonalSquadRoot()` in `consult.ts` used `.squad` as subdirectory, while `resolvePersonalSquadDir()` and `ensurePersonalSquadDir()` in `resolution.ts` use `personal-squad`. Path mismatch broke all personal squad discovery.

**Fix:**
- Updated `consult.ts` to use `personal-squad` subdirectory
- Added 2 new test cases covering both functions
- Confirmed template sync script propagated changes

**Decision:** Personal squad paths must use `personal-squad/` subdirectory (canonical). `.squad` is reserved for project-local squad directories only.

---

*Logged by Scribe at 2026-03-25T18:11Z*
